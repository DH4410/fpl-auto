# FPL bot dependencies (bot/ package + notebooks/fpl_bot_v1.ipynb).
# Install alongside the web app's requirements.txt:
#     pip install -r requirements.txt -r requirements_bot.txt
#
# NO API KEYS ARE REQUIRED BY ANYTHING HERE. Every data source the bot uses is a
# public, unauthenticated HTTP GET (the official FPL API, Vaastav's GitHub CSVs
# and football-data.co.uk). There are no accounts, keys or subscriptions.

# --- core numerics -----------------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0          # Dixon-Coles MLE, Shin de-vigging, Poisson grids
scikit-learn>=1.3.0

# --- gradient boosting (models.py) -------------------------------------------
xgboost>=2.0.0         # minutes (custom asymmetric objective), defense, bonus
lightgbm>=4.0.0        # attack model (xG/90, xA/90)

# --- optimisation (optimizer.py) ---------------------------------------------
# PuLP ships with the CBC solver. highspy adds HiGHS, which is markedly faster
# on these models; optimizer.get_solver() probes for it and falls back to CBC.
# gurobipy is deliberately absent -- it is commercial and is never imported.
pulp>=2.7.0
highspy>=1.5.3

# --- reinforcement learning (rl_agent.py) ------------------------------------
# Optional: the rest of the bot runs without these and raises a clear install
# message if the chip agent is used. stable-baselines3 pulls in torch (~2 GB).
gymnasium>=0.29.0
stable-baselines3>=2.1.0
torch>=2.0.0

# --- survival analysis -------------------------------------------------------
# Reserved for the injury/return-date hazard model described in
# research/starting_point/README.md. Not yet imported by any module.
lifelines>=0.27.0

# --- visualisation (notebook only) -------------------------------------------
matplotlib>=3.7.0
seaborn>=0.12.0

# --- notebook ----------------------------------------------------------------
jupyter>=1.0.0
ipykernel>=6.25.0

# --- http --------------------------------------------------------------------
# Also in the main requirements.txt; repeated so the bot installs standalone.
requests>=2.31.0

# --- optional free enrichment ------------------------------------------------
# soccerdata scrapes FBref/Understat public pages (no key). Uncomment to enable
# data_collector.fetch_soccerdata_fbref; it is heavy and self-rate-limits, and
# nothing in the pipeline depends on it.
# soccerdata>=1.6.0
